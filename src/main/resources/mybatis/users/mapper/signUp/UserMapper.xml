<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.practice.bookApi.users.sign.mapper.UserMapper">
	
	<insert id="insertUser" parameterType="com.practice.bookApi.users.sign.dto.UserDto">
		INSERT INTO ba_user(
							user_id,
							user_pw,
							user_name,
							user_mail,
							gender,
							birthday,
							user_addr1,
							user_addr2
							)
					VALUES(
							#{userId},
							#{userPw},
							#{userName},
							#{userMail},
							#{gender},
							#{birthday},
							#{userAddr1},
							#{userAddr2}
							)
	</insert>
	
	<select id="idcheck" parameterType="String" resultType="Integer">
		SELECT COUNT(*) FROM ba_user
		WHERE
			user_id = #{userId}
	</select>
	
	<select id="mailcheck" parameterType="String" resultType="Integer">
		SELECT COUNT(*) FROM ba_user
		WHERE
			user_mail = #{userMail}
	</select>
	
	<select id="userCheck" resultType="com.practice.bookApi.users.sign.dto.UserDto">
		SELECT * FROM ba_user
		WHERE 
			user_id = #{userId}
	</select>
	
	<select id="findIdByEmail" resultType="com.practice.bookApi.users.sign.dto.UserDto">
		SELECT * FROM ba_user
		WHERE 
			user_mail = #{userMail}
	</select>
	
	<select id="getUserSessionId" resultType="com.practice.bookApi.users.sign.dto.UserDto">
		SELECT * FROM ba_user
		WHERE 
			session_id = #{sessionId}
	</select>
	
	<select id="findByIdAndEmail" resultType="com.practice.bookApi.users.sign.dto.UserDto">
	    SELECT * FROM ba_user
	    WHERE user_id = #{userId}
	      AND user_mail = #{userMail}
	</select>
	
	<select id="selectUserNameById" resultType="String">
		SELECT user_name FROM ba_user
		WHERE	
			user_id = #{userId}
	</select>
	
	<select id="findUserNoByUserId" resultType="long">
	    SELECT user_no FROM ba_user
	    WHERE 
	    	user_id = #{userId}
	</select>
	
	<update id="keepLogin">
		UPDATE ba_user 
		SET session_id = #{sessionId},
		 	limit_time = #{limitDate},
		 	modified_date = NOW()
		WHERE
			user_id = #{userId}
	</update>
	
	<update id="updatePassword">
		UPDATE ba_user
		SET user_pw = #{newPw},
			modified_date = NOW()
		WHERE user_id = #{userId}
	</update>
	
	<update id="updateFindPassword">
	    UPDATE ba_user
	    SET user_pw = #{newPw}, modified_date = NOW()
	    WHERE user_id = #{userId} AND user_mail = #{userMail}
	</update>
	
	
	
</mapper>